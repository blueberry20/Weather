A Weather App built in React with Create React App and OpenWeatherMap API.

=================================================
To run the project locally:

> git clone https://github.com/blueberry20/Weather.git
> cd Weather
> npm install
> npm start